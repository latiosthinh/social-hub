services:
  socialhub:
    build: .
    container_name: socialhub-app
    ports:
      - "3240:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/broadcaster.db
    volumes:
      - socialhub:/app/data
    restart: always

volumes:
  socialhub:
    external: true
